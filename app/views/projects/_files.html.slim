.file-list
  h6
    | Video 
    small= "(#{project.file_uploads.count})"

  ul
    - project.file_uploads.each do |upload|
      li= link_to upload.file_name, upload.url
.file-list
  h6
    | Music 
    small= "(#{project.activid_music_urls.count})"

  ul
    - project.activid_music_urls.each do |url|
      li= link_to url.split("/").last, url
